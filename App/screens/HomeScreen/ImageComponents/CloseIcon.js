import React from 'react';
import Close from '../../../assets/images/close_white.svg';

const CloseIcon = (props) => {
  return <Close height={25} width={25} {...props} />;
};

export default CloseIcon;
