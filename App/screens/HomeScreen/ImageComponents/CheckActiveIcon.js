import React from 'react';
import CheckIcon from '../../../assets/images/check_active.svg';
const Active = (props) => {
  return <CheckIcon height={25} width={25} {...props} />;
};

export default Active;