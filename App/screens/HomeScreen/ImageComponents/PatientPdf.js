import React from 'react';
import PdfIcon from '../../../assets/images/pdf.svg';
const PatientPdf = (props) => {
  return <PdfIcon height={15} width={15} {...props} />;
};

export default PatientPdf;
