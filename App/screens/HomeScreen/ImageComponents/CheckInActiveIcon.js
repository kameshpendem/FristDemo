import React from 'react';
import CheckIcon from '../../../assets/images/check_inactive.svg';
const Close = (props) => {
  return <CheckIcon height={25} width={25} {...props} />;
};

export default Close;
